\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{float}
\usepackage[most]{tcolorbox}
\usepackage{xcolor} % Explicitly load xcolor for HTML colors

% --- 1. Define the Environments (MISSING PART) ---
\newtheorem{theorem}{Theorem}[section]      %%% <--- NEW
\newtheorem{lemma}[theorem]{Lemma}          %%% <--- NEW
\newtheorem{corollary}[theorem]{Corollary}  %%% <--- NEW
\newtheorem{proposition}[theorem]{Proposition} %%% <--- NEW
\newtheorem{definition}{Definition}[section] %%% <--- NEW
\newtheorem{example}{Example}[section]      %%% <--- NEW

% Define 'sol' (Solution) environment
\newenvironment{sol}                        %%% <--- NEW
  {\begin{proof}[Solution]}                 %%% <--- NEW
  {\end{proof}}                             %%% <--- NEW

% --- 2. Fix the Algorithm Definition ---
% Changed \renewenvironment to \newenvironment because 'algorithm'
% didn't exist yet.
\newenvironment{algorithm}[1][]{%          %%% <--- CHANGED from \renew to \new
  \begin{list}{}{%
    \leftmargin=1em
    \rightmargin=1em
  }%
  \item\relax
  \textbf{Algorithm: #1}\par\medskip
}{%
  \end{list}
}

% --- Fix Horizontal Rules (---) ---
\let\oldrule\rule
\renewcommand{\rule}[2]{%
  \ifdim#1=0.5\linewidth
    \hrule
  \else
    \oldrule{#1}{#2}
  \fi
}

% --- Force Equation Numbering ---
\renewcommand{\[}{\begin{equation}}
\renewcommand{\]}{\end{equation}}

% --- Color Definitions ---
\definecolor{thm-bg}{HTML}{F0F7FF}
\definecolor{thm-frame}{HTML}{CFE2FF}
\definecolor{def-bg}{HTML}{F0FFF4}
\definecolor{def-frame}{HTML}{BADBCC}
\definecolor{ex-bg}{HTML}{FFFBF0}
\definecolor{ex-frame}{HTML}{FFEEBB}
\definecolor{proof-bg}{HTML}{E2E3E5}
\definecolor{proof-frame}{HTML}{E2E3E5}
\definecolor{sol-bg}{HTML}{F9F2F4}
\definecolor{sol-frame}{HTML}{D9534F}
\definecolor{alg-bg}{HTML}{F3E5F5}
\definecolor{alg-frame}{HTML}{CE93D8}

% --- Box Environments ---

% Now that 'theorem' exists, we can style it:
\tcolorboxenvironment{theorem}{
  colback=thm-bg,
  colframe=thm-frame,
  boxrule=0pt,
  sharp corners,
  left=5pt, right=5pt,
  top=5pt, bottom=5pt,
  breakable
}

\tcolorboxenvironment{lemma}{
  colback=thm-bg,
  colframe=thm-frame,
  boxrule=0pt,
  sharp corners,
  left=5pt, right=5pt,
  top=5pt, bottom=5pt,
  breakable
}

\tcolorboxenvironment{corollary}{
  colback=thm-bg,
  colframe=thm-frame,
  boxrule=0pt,
  sharp corners,
  left=5pt, right=5pt,
  top=5pt, bottom=5pt,
  breakable
}

\tcolorboxenvironment{proposition}{
  colback=thm-bg,
  colframe=thm-frame,
  boxrule=0pt,
  sharp corners,
  left=5pt, right=5pt,
  top=5pt, bottom=5pt,
  breakable
}

\tcolorboxenvironment{definition}{
  colback=def-bg,
  colframe=def-frame,
  boxrule=0pt,
  sharp corners,
  left=5pt, right=5pt,
  top=5pt, bottom=5pt,
  breakable
}

\tcolorboxenvironment{example}{
  colback=ex-bg,
  colframe=ex-frame,
  boxrule=0pt,
  sharp corners,
  left=5pt, right=5pt,
  top=5pt, bottom=5pt,
  breakable
}

\tcolorboxenvironment{proof}{
  colback=proof-bg,
  colframe=proof-frame,
  boxrule=0pt,
  sharp corners,
  left=5pt, right=5pt,
  top=5pt, bottom=5pt,
  breakable
}

% Now that 'sol' exists, we can style it:
\tcolorboxenvironment{sol}{
  colback=sol-bg,
  colframe=sol-frame,
  boxrule=0pt,
  sharp corners,
  left=5pt, right=5pt,
  top=5pt, bottom=5pt,
  breakable
}

% Now that 'algorithm' exists, we can style it:
\tcolorboxenvironment{algorithm}{
  colback=alg-bg,
  colframe=alg-frame,
  boxrule=0pt,
  sharp corners,
  left=5pt, right=5pt,
  top=5pt, bottom=5pt,
  breakable
}
