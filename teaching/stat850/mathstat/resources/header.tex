% 1. Clear Quarto's potential default definition (optional, but safer)
\makeatletter
\let\theorem\relax
\let\endtheorem\relax
\makeatother

% 2. Load packages if needed (Quarto usually loads amsthm)
\usepackage{amsthm}
\usepackage{tcolorbox}

% 3. Define your own style
% Example: A shaded box for theorems
\newtcolorbox{theorem}[1][]{
  colback=blue!5!white,
  colframe=blue!75!black,
  title=Theorem #1
}

% Example: A simple bold label for solutions
\renewenvironment{solution}
  {\par\noindent\textbf{My Custom Solution:}\ \ignorespaces}
  {\par}

\let\oldrule\rule
\renewcommand{\rule}[2]{\ifdim#1=0.5\linewidth\hrule\else\oldrule{#1}{#2}\fi}
\renewcommand{\[}{\begin{equation}}
\renewcommand{\]}{\end{equation}}  